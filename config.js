/* LISTO AI - config de front-end
   EM PRODUCAO: troque a linha abaixo pela URL do seu backend (Render/Cloudflare Worker).
   Ex: window.LISTO_API = 'https://listoai.onrender.com';
   Em modo local (dev), deixe como esta. */
window.LISTO_API = 'http://127.0.0.1:8021';
